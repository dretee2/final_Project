[pytest]
json_report = report/json/report.json
markers =
    login: mark as login tests
    home: mark as home tests
    regression: mark as tests for regression
    settings: mark as tests for the settings page
addopts = -vs -rf --html-report=./report